{
  "name": "backend",
  "version": "0.0.1",
  "scripts": {
    "build": "rimraf ./dist && npx --package=typescript -- tsc --build tsconfig.json",
    "dev": "ts-node-dev --respawn --quiet src/index.ts",
    "start": "npm i && node dist/index.js"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/mysql": "^2.15.21",
    "rimraf": "^3.0.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@fastify/cookie": "^8.1.0",
    "@fastify/middie": "^8.0.0",
    "@fastify/static": "^6.5.0",
    "bcrypt": "^5.0.1",
    "dotenv": "^16.0.1",
    "email-validator": "^2.0.4",
    "fastify": "^4.6.0",
    "mysql": "^2.18.1"
  }
}
